2026-09-19 version 2.1.9-web.74 (current series):
- new: Kannada (ಕನ್ನಡ) is now available in the language list - the whole interface, the built-in help and the welcome notices are translated, bringing the client to 59 languages

2026-09-19 version 2.1.9-web.73:
- new: the public pages (poker rules, hand rankings, glossary, how to play, FAQ) are now available in Marathi too
- bugfix: in the Malayalam poker glossary, the "Freeroll" entry carried the definition of another term - it now explains what a freeroll is

2026-09-19 version 2.1.9-web.72:
- new: Marathi (मराठी) is now available in the language list - the whole interface, the built-in help and the welcome notices are translated, bringing the client to 58 languages

2026-09-19 version 2.1.9-web.71:
- new: the public pages (poker rules, hand rankings, glossary, how to play, FAQ) are now available in Malayalam too

2026-09-19 version 2.1.9-web.70:
- new: Malayalam (മലയാളം) is now available in the language list - the whole interface, the built-in help and the welcome notices are translated, bringing the client to 57 languages

2026-09-19 version 2.1.9-web.69:
- bugfix: the welcome notice for registered players said ranking games start with blinds at 55 in Afrikaans, Filipino, Indonesian, Lithuanian, Turkish and Traditional Chinese - it is 50, as in every other language

2026-09-19 version 2.1.9-web.68:
- new: the public pages (poker rules, hand rankings, glossary, how to play, FAQ) are now available in Telugu too

2026-09-19 version 2.1.9-web.67:
- new: Telugu (తెలుగు) is now available in the language list - the whole interface, the built-in help and the welcome notices are translated, bringing the client to 56 languages

2026-09-19 version 2.1.9-web.66:
- improvement: mobile zoom (magnifier) now behaves like the Android app - on your turn it shows your own area at once, on a new community card it moves to the board, it jumps to a player as soon as he acts, and after a showdown the next hand starts centred

2026-09-19 version 2.1.9-web.65:
- bugfix: mobile zoom (magnifier) - with "Remove departed players" enabled, the zoomed view no longer stays stuck on an empty spot when a player leaves the table; it follows the seat it was showing, or returns to the table centre (same fix as the desktop client)

2026-09-19 version 2.1.9-web.64:
- bugfix: training mode - when the table gets down to two players, nobody has to post the big blind twice in a row any more (same fix as the desktop client)

2026-09-18 version 2.1.9-web.62:
- improvement: the game starts faster and uses far less data - only your language is downloaded instead of all 55 (the others load when you pick them)
- bugfix: on an unstable connection the game could fail to start because one of the 55 language files did not load - it now retries, and falls back to English rather than not starting

2026-09-17 version 2.1.9-web.60:
- improvement: the avatar Import tab now says clearly that any image works - it is cropped and resized automatically, you do not need to prepare a 96x96 file yourself
- improvement: the "Restore defaults" settings category is now called "Backup & reset", so the config.xml and backup import/export buttons are easier to find
- bugfix: the help pointed to the wrong settings category for the config.xml import/export

2026-09-15 version 2.1.9-web.59:
- bugfix: the community cards in portrait mode are now closer in size to the desktop client

2026-09-15 version 2.1.9-web.58:
- bugfix: folded players no longer show a red X on their avatar - the fold badge and dimmed cards already make it clear

2026-09-15 version 2.1.9-web.57:
- bugfix: training mode could fail to start when you had no connection, since the bots' chat lines were not stored for offline use

2026-09-15 version 2.1.9-web.55:
- improvement: the bots' chat one-liners are now translated into all 55 interface languages, not just English and French

2026-09-15 version 2.1.9-web.54:
- new: bots now sometimes speak up in chat with personality-matched one-liners (a boast, a groan, a "gg") instead of only emoji reactions - available in English and French for now, other languages coming soon

2026-09-15 version 2.1.9-web.51:
- bugfix: the mini music widget is now properly narrow on phones too, not just on desktop
2026-09-15 version 2.1.9-web.50:
- improvement: adjusted the music player based on feedback - the like/dislike buttons now sit next to the time counter, and the mini widget is narrower
2026-09-15 version 2.1.9-web.49:
- improvement: the mini music player now has like/dislike buttons, and the full player is more compact
2026-09-15 version 2.1.9-web.48:
- improvement: the card flip animation on the table now matches the desktop client exactly
2026-09-14 version 2.1.9-web.47:
- improvement: on the live spectator page (/live), opening a table's details now closes any other one you had open, so only one shows at a time
2026-09-14 version 2.1.9-web.46:
- bugfix: fixed the table jittering during actions and oversized seats on the Boardwalk, High Roller and Onyx Pill seat styles
2026-09-14 version 2.1.9-web.45:
- bugfix: the flop/turn/river cards are now grouped and spaced exactly like the desktop client, and the pot marker now uses the same coin icon as your bet chips
2026-09-14 version 2.1.9-web.44:
- bugfix: fixed two table display bugs - a new game with fewer players than the previous one could keep the old seat layout, and the pot marker could grow too big or overlap the top player's box on some tables
2026-09-14 version 2.1.9-web.43:
- new: the poker rules and FAQ pages are now available in Estonian, Latvian, Slovenian, Bosnian, Macedonian, Malay, Albanian, Punjabi, Amharic and Khmer - every language now has them
2026-09-14 version 2.1.9-web.42:
- improvement: the help pages, public pages and FAQ now all state the correct number of interface languages (55) in every language
2026-09-14 version 2.1.9-web.41:
- new: the public pages (poker rules, hand rankings, glossary, how to play) are now available in Khmer too
2026-09-14 version 2.1.9-web.40:
- new: the built-in help and the welcome notices are now available in Khmer too
2026-09-14 version 2.1.9-web.39:
- new: Khmer (ខ្មែរ) is now available in the language list - the whole interface is translated, bringing the client to 55 languages
2026-09-14 version 2.1.9-web.38:
- bugfix: the mute button in the lobby chat header wasn't showing up - a leftover style rule was hiding it by mistake
2026-09-14 version 2.1.9-web.37:
- new: a small bell icon next to the trash icon in the chat header (table chat and lobby chat) lets you mute that chat's notification sound - each one is independent
2026-09-14 version 2.1.9-web.36:
- bugfix: the Tables/Players tab bar on the live spectator page (/live) now stays properly pinned to the top while scrolling in every browser, not just some
2026-09-14 version 2.1.9-web.35:
- bugfix: on the live spectator page (/live), expanding a table row no longer causes the Tables/Players tab bar to briefly flash stale content
2026-09-14 version 2.1.9-web.34:
- bugfix: on iPhone, a sliver of the previous table row no longer peeks above the Tables/Players tab bar on the live spectator page (/live) while scrolling
2026-09-14 version 2.1.9-web.33:
- bugfix: on the live spectator page (/live), the Tables/Players tab bar no longer looks see-through when it's pinned at the top while you scroll
2026-09-14 version 2.1.9-web.32:
- bugfix: on the live spectator page (/live), a folded player's "Fold" tag really stays fully readable now - the previous fix missed that the tag sits inside the same box as the cards
2026-09-14 version 2.1.9-web.31:
- improvement: the theme button on the live spectator page (/live) now only switches between light and dark - the confusing third "automatic" step is gone
2026-09-14 version 2.1.9-web.30:
- bugfix: on the live spectator page (/live), the Tables/Players tab bar now stays put while you scroll the list instead of scrolling away with it
2026-09-14 version 2.1.9-web.29:
- improvement: the "Currently idle" label on the live spectator page (/live) is now translated in all 54 languages instead of just English and French
2026-09-14 version 2.1.9-web.28:
- new: the live spectator page (/live) now shows "Currently idle" for players who aren't seated or watching a table, like the old spectator tool did
2026-09-14 version 2.1.9-web.27:
- bugfix: on the live spectator page (/live), a folded player's name and avatar no longer grey out along with their "Fold" tag - only their cards do, so the tag stays easy to read
2026-09-14 version 2.1.9-web.26:
- bugfix: the live spectator page (/live) no longer plays a chat notification sound for every single message - it was going off constantly instead of just occasionally
2026-09-14 version 2.1.9-web.25:
- bugfix: the admin editors for the welcome message, notices and polls now list all 54 languages (Amharic, Bengali, Filipino, Indonesian, Punjabi, Swahili and Thai were missing from the picker)
2026-09-14 version 2.1.9-web.24:
- new: the public pages (poker rules, hand rankings, glossary, how to play) are now available in Amharic too
2026-09-14 version 2.1.9-web.23:
- new: the built-in help and the welcome notices are now available in Amharic too
2026-09-14 version 2.1.9-web.22:
- new: Amharic (አማርኛ) is now available in the language list - the whole interface is translated, bringing the client to 54 languages
2026-09-14 version 2.1.9-web.21:
- new: the public pages (poker rules, hand rankings, glossary, how to play) are now available in Punjabi too
2026-09-13 version 2.1.9-web.20:
- new: the built-in help and the welcome notices are now available in Punjabi too
2026-09-13 version 2.1.9-web.19:
- new: Punjabi (ਪੰਜਾਬੀ) is now available in the language list - the whole interface is translated, bringing the client to 53 languages
2026-09-13 version 2.1.9-web.18:
- bugfix: the live spectator tool (/live) could pop up a game-invite banner if another player invited the spectator - it's now silenced there, since a spectator can't join a game anyway.
2026-09-13 version 2.1.9-web.17:
- new: the public pages (poker rules, hand rankings, glossary, how to play) and the welcome notices are now available in Albanian too
2026-09-13 version 2.1.9-web.16:
- new: Albanian (Shqip) is now available in the language list - the whole interface and the built-in help are translated, bringing the client to 52 languages
2026-09-13 version 2.1.9-web.15:
- bugfix: the live spectator tool's Players tab no longer shows the same player several times after they reconnect
2026-09-13 version 2.1.9-web.14:
- new: the public pages (poker rules, hand rankings, glossary, how to play) and the welcome notices are now available in Malay too
2026-09-13 version 2.1.9-web.13:
- new: Malay (Bahasa Melayu) is now available in the language list - the whole interface and the built-in help are translated, bringing the client to 51 languages
2026-09-13 version 2.1.9-web.12:
- new: the public pages (poker rules, hand rankings, glossary, how to play) and the welcome notices are now available in Macedonian too
2026-09-13 version 2.1.9-web.11:
- new: Macedonian (Македонски) is now available in the language list - the whole interface and the built-in help are translated, bringing the client to 50 languages
2026-09-13 version 2.1.9-web.10:
- new: the public pages (poker rules, hand rankings, glossary, how to play) and the welcome notices are now available in Bosnian too
2026-09-13 version 2.1.9-web.9:
- new: Bosnian (Bosanski) is now available in the language list - the whole interface and the built-in help are translated, bringing the client to 49 languages
2026-09-13 version 2.1.9-web.8:
- bugfix: the guest-mode and registered-account welcome notices, and the LAN/dedicated-server notice, are now available in Estonian and Latvian too (they had been missing since those languages launched).
2026-09-13 version 2.1.9-web.7:
- bugfix: the guest-mode and registered-account welcome notices weren't available in Slovenian yet, even though the rest of the interface was - fixed.
2026-09-13 version 2.1.9-web.6:
- new: the public pages (poker rules, hand rankings, glossary, how to play) are now available in Slovenian too, so a search in Slovenian lands on a Slovenian page instead of the English one
2026-09-13 version 2.1.9-web.5:
- new: Slovenian (Slovenščina) is now available in the language list - the whole interface and the built-in help are translated, bringing the client to 48 languages
2026-09-13 version 2.1.9-web.4:
- bugfix: the Changelog tab used to show one small block per build, which made recent updates look scattered; each update to this same overall version now reads as a single clean entry, grouped by New / Improvements / Bug fixes.
2026-09-13 version 2.1.9-web.3:
- bugfix: table styles with their own action buttons (Ivoire & Chêne, Casino, imported skins) could quietly fall back to the default plain buttons after reloading the page. They now stick.
- bugfix: removed an extra floating chat button in the corner - the one on the table felt already does the job.
2026-09-13 version 2.1.9-web.2:
- new: if this server only allows one LAN/dedicated address, you'll now see a short note explaining that and pointing you to running your own copy of the web client if you want to connect elsewhere (off by default, set up by the server admin).
2026-09-13 version 2.1.9-web.1:
- bugfix: if your avatar image can't be sent to the table (too complex to fit the server's size limit once re-encoded, or an unreadable file), you now get a clear message instead of it silently stopping to show up on future connections.
2026-09-12 version 2.1.9-web.0:
- improvement: the chances panel is easier to read - categories that are impossible with your current cards now stay visible instead of nearly disappearing, and the odds bars themselves are a bit more visible too.
2026-09-12 version 2.1.8-web.158:
- bugfix: the reactions window's page switcher (‹ 1/3 ›) now follows the table style's colours too, matching the title fix from the previous update.
2026-09-12 version 2.1.8-web.157:
- bugfix: the reactions window's title now follows the table style's text colour like the chat/log windows do, instead of always staying the app's default gold.
2026-09-12 version 2.1.8-web.156:
- bugfix: tabbing through the login screen with the keyboard could eventually scroll down to a floating chat button that only makes sense at the poker table. It now only shows up during a game.
2026-09-12 version 2.1.8-web.155:
- bugfix: right after an update, the app could briefly mix a bit of the new version with a bit of the old one (for example a new button showing up without its proper styling) until you reloaded a second time. Updates now always apply as a clean, complete set.
2026-09-12 version 2.1.8-web.154:
- bugfix: in the online players list, hiding a column (avatar, in-game, country, actions) no longer makes the name column jump to an unpredictable width - the header buttons and the list now always line up, however many columns you hide and whatever order you hide them in. Hidden columns move into a small "+" button so you can bring them back.
2026-09-12 version 2.1.8-web.153:
- improvement: the accessibility button now sits in the same spot in every header (right before the settings/overflow icon) - it was out of place on the login screen and missing altogether from the create-table screen
2026-09-12 version 2.1.8-web.149:
- new: the public pages (poker rules, hand rankings, glossary, how to play) are now available in Latvian too, so a search in Latvian lands on a Latvian page instead of the English one
2026-09-12 version 2.1.8-web.148:
- new: Latvian (Latviešu) is now available in the language list - the whole interface and the built-in help are translated, bringing the client to 47 languages
2026-09-12 version 2.1.8-web.147:
- bugfix: reopening the chat or info drawer via the Accessibility panel at Extra Large no longer leaves keyboard focus stuck outside it - it now lands back in the drawer as intended
2026-09-12 version 2.1.8-web.146:
- bugfix: the chat and info drawer buttons in mobile landscape at Extra Large interface size are properly tappable again - they had shrunk below the accessible touch-target size
2026-09-12 version 2.1.8-web.145:
- new: the public pages (poker rules, hand rankings, glossary, how to play) are now available in Estonian too, so a search in Estonian lands on an Estonian page instead of the English one
2026-09-12 version 2.1.8-web.144:
- new: Estonian (Eesti) is now available in the language list - the whole interface and the built-in help are translated, bringing the client to 46 languages
2026-09-11 version 2.1.8-web.143:
- bugfix: action icons (message, ignore, stats) in the online players list are properly lined up under their header icon again - a leftover invisible column was pushing them out of place
2026-09-11 version 2.1.8-web.142:
- bugfix: forum posts you already read no longer reappear as unread just because someone replied to the same topic
2026-09-11 version 2.1.8-web.141:
- bugfix: hiding a column (in-game status, country, rating) in the online players list now actually gives the name more room - it used to just clear the icon while the same space stayed reserved
2026-09-11 version 2.1.8-web.140:
- bugfix: the Local/Training and LAN tabs in the Player profile window now show your real stats and all their sub-tabs (including reset) no matter which mode you're currently connected in - they used to look empty when the mode didn't match
2026-09-11 version 2.1.8-web.139:
- improvement: the Player profile window now shows "Local / Training" and "LAN" as two separate tabs, always visible together - LAN keeps all 3 sub-tabs (Session, Total, and the family ranking with its reset button)
2026-09-11 version 2.1.8-web.138:
- improvement: the Player profile window (from your own avatar) is now organized as three tabs - Cups, Local/Training, and LAN - each shown only when it applies, instead of a stats box always stacked above the cups
2026-09-11 version 2.1.8-web.137:
- bugfix: in LAN or private-server games, clicking your own avatar now lets you open your statistics (score, total stats) and reset them again - this button had accidentally only ever worked when connected to pokerth.net
2026-09-11 version 2.1.8-web.136:
- new: the Table ranking window (podium button) now has a LAN tab when you're connected to a LAN or private-server game - it shows the family leaderboard for just the players sitting at this table
2026-09-11 version 2.1.8-web.135:
- improvement: pressing Escape now closes the Accessibility panel first if it's open over another screen, such as the login form, instead of acting on that screen underneath - this closes out the Accessibility feature (interface size, high contrast, entry points, mobile layouts); thanks to @seanpianka for the contribution
2026-09-11 version 2.1.8-web.134:
- new: the High contrast toggle in the Accessibility panel now actually changes how the app looks - a dedicated high-contrast palette for the interface, felt, and cards; turning it off brings back your exact previous look. This was the last accessibility option still waiting for its on-screen effect - the panel is now fully working end to end
2026-09-11 version 2.1.8-web.132:
- improvement: the adaptive layout for large interface sizes now also covers landscape play, and the Hands and Reactions buttons in its bottom bar now open in the right place (this closes the spot mentioned in the previous update)
- bugfix: pressing Escape on the reactions panel now closes it, like every other panel - it used to toggle "keep window open" instead
2026-09-11 version 2.1.8-web.127:
- new: at Extra Large interface size, portrait play now uses an adapted layout - chat and log open as full-screen panels reachable from a bar at the bottom, and the round table buttons make room for it (note: for now the Hands and Reactions buttons in that bar can still open in the wrong spot - a fix is coming very shortly)
2026-09-11 version 2.1.8-web.124:
- improvement: the Large and Extra Large interface sizes from the Accessibility panel now also scale up the desktop table during a game (seats, badges, whose turn it is) - the table geometry is unchanged if you leave the setting at Standard
2026-09-11 version 2.1.8-web.123:
- improvement: the Large and Extra Large interface sizes from the Accessibility panel now actually enlarge the login screen and the lobby, including the table list once it's populated (high contrast still has no visible effect yet - it's coming in a later update)
2026-09-11 version 2.1.8-web.122:
- new: an Accessibility button is now available before login and in the lobby and game headers, opening a panel where you can pick an interface size, turn on a high-contrast mode, and set the browser pinch-zoom permission - your choices are saved and follow you across devices with settings sync (the on-screen effect of interface size and high contrast is coming in the next updates)
2026-09-11 version 2.1.8-web.121:
- bugfix: reopening the app could show the previous version for a moment with no update prompt, especially after it had been in the background a while; it now catches up and reloads once by itself
2026-09-10 version 2.1.8-web:
- new: in LAN and private-server games, the ranking window (trophy button) has a LAN tab, opened by default - it lists every player of the server and can be sorted by net winnings, winnings per 100 hands, win rate, games won or best streak
- improvement: in training mode the cards are now shuffled, and the bots decide, with the browser's cryptographic random generator, as in the desktop client and on the server - the cards still to come and the bots' hidden cards can no longer be worked out from the ones already shown
- improvement: more of the client works from the keyboard, as in the desktop client - About, privacy, table ranking and player pages scroll with Page Up/Down, Home and End as soon as they open; the ranking opens in its search box and Enter searches at once; the arrow keys move through the settings categories and the forum news list, and back from a post returns to it; the end-of-game window starts on Play again in training and Escape closes it; Escape on the login form goes back to the choice of mode; Ctrl+Enter saves a player note
- improvement: with a keyboard, each screen now starts with the focus where you need it, as in the desktop client - the Internet card on the opening screen, your nickname (or your password once the nickname is filled) on the login step, the table name when creating a game and the Create button in training - and Enter in any field of the game settings creates the table
- bugfix: in the live spectator view on pokerth.net, a long list of tables was cut off at the bottom with no way to scroll; the list scrolls again, and a watched table uses the whole height of the view instead of a squeezed strip
- new: the lobby table list works with the keyboard, as in the desktop client - reach it with Tab, move between tables with the arrow keys (Home/End for the first and last), and press Enter to join the selected table (on a narrow screen, Enter shows its details); the keyboard shortcuts page of the help lists it
- improvement: with a keyboard, the kick and report confirmations now open with the focus on the confirm button, as in the desktop and mobile clients - Enter confirms, Tab reaches Cancel, Escape still cancels
- improvement: with a keyboard, windows that pop up now put the focus on the safe button, as in the desktop and mobile clients - Decline on a game invitation (Escape declines too), Cancel when leaving a game or disconnecting, OK on the inactivity and connection-lost messages - so an Enter pressed by accident never takes you anywhere; Tab reaches the other button, and the focus goes back where it was when the window closes
- improvement: emoji reactions now play at the same pace as in the desktop and mobile clients - each animation lasts a quarter longer (about 2 seconds) and the bomb explodes as it lands, so a reaction looks the same to every player at the table
- improvement: anonymous visit identifiers of devices not seen for about 13 months are now deleted too, as the privacy page says
- bugfix: the error report option now says exactly what is sent (error details, browser and shortened IP address) instead of "no personal data"
- bugfix: the settings sync option no longer says it is opt-in - it is on by default for registered accounts, as the privacy page explains
- bugfix: the table picture could jump by one pixel during a hand, for example right after your turn; it now stays still and only moves when the window size or the table style changes
- improvement: the privacy page now lists everything the web client sends and where - settings sync, error reports, polls, translation, radios - and tells you to contact the pokerth.net administrators for any data request
- improvement: the default cards and the default PokerTH table (felt, dealer and blind buttons, action buttons) are now stored with the app, so an offline training game shows them without a connection, even right after an update

2026-09-10 version 2.1.8-web:
- bugfix: once the web client had been updated, the offline training mode against bots could refuse to start without a connection ("Offline init failed") until it had been opened online once; everything it needs is now stored with the app on install, so it starts with no network at all

2026-09-10 version 2.1.8-web:
- bugfix: a palette button and an empty button had appeared in the lobby and table headers; they belonged to the spectator view on pokerth.net only and are gone from the web client
- bugfix: the same player could show one avatar to web players and another to players on the desktop and mobile clients - a picture changed while connected, or brought back with a backup, reached web players only; the avatar now goes through the server alone, as in the official clients, so everyone sees the same one
- improvement: an avatar chosen while you are connected now takes effect from your next connection, as in the desktop and mobile clients, instead of changing on some screens only
- improvement: the initial-letter avatar (Aa) is no longer sent as a picture; other players see the default avatar, as they do for a desktop player who has none

2026-09-09 version 2.1.8-web:
- bugfix: when the inactivity countdown ran out, the warning window stayed on screen with a dead OK button and the lobby behind it never moved, because the disconnection from the server sometimes never reached the browser; the client now ends the session itself a few seconds later and takes you back to the connection screen

2026-09-09 version 2.1.8-web:
- bugfix: when the server ended the session - kicked, banned, or timed out after 20 minutes without activity - the lobby stayed on screen, frozen, with no word of what had happened; the client now returns to the connection screen and says why, like the desktop and mobile clients

2026-09-09 version 2.1.8-web:
- bugfix: in the lobby, a player's picture was missing from the online players list on the left - it stayed a plain letter while the very same player showed their picture in the game info panel; pictures that finish downloading now appear everywhere at once

2026-09-09 version 2.1.8-web:
- bugfix: in the About window, the web changelog repeated the same version heading - and its "New", "Improvements" and "Bug fixes" titles - once per deployment; every entry of a version is now gathered under a single heading, the way the changelog of the installable clients reads

2026-09-09 version 2.1.8-web:
- bugfix: the live figures on the opening screen could blow the Internet card up to the full height of the screen on a browser still holding an older stylesheet - the icons now carry their own size and no longer depend on it

2026-09-09 version 2.1.8-web:
- improvement: the live figures on the opening screen now show all four counters the website does - players online, tables running, players waiting and games today - as an icon and a number each, so nothing gets cut off on a narrow phone screen

2026-09-08 version 2.1.8-web:
- improvement: the Ivoire & Chene table now uses the finer colours of the official client - the chat, log and info boxes sit a shade deeper than the felt, so light things on them (emoji, avatars) keep an edge, and the muted text, the side-pot winner and the send symbol read more clearly on cream

2026-09-08 version 2.1.8-web:
- new: the Internet card on the opening screen now shows how many players are on pokerth.net and how many games have been played today, refreshed while you look at it, so you can tell whether the tables are busy before you connect

2026-09-08 version 2.1.8-web:
- new: the game log now colours its lines the way the desktop and mobile clients do - the winner of the pot, the winner of a side pot, and the flop/turn/river steps each have their own colour, so a hand can be read at a glance instead of line by line
- improvement: on a light table style such as Ivoire & Chene, the chat and log colours are the darkened set the official client uses there, instead of the yellow and orange meant for a dark felt
- improvement: an imported table style can now set those colours itself (accent, winner, side pot, board, send), like the styles shipped with the official client; anything it leaves out is filled in to match the brightness of its own panel background

2026-09-08 version 2.1.8-web:
- bugfix: playing on a LAN or dedicated server works again from anywhere - the client was dialling the wrong address for the relay and never reached it, so the connection failed with "is the proxy running?" no matter which server you typed
- improvement: a LAN game now always goes through the site's relay, and only the Internet connection carries the operator's real-IP header - the two no longer get in each other's way

2026-09-08 version 2.1.8-web:
- improvement: when the site turns a connection away - too many tabs open, too many attempts in a row, or a blocked address - the server operator can now see it in the logs instead of nothing at all

2026-09-07 version 2.1.8-web:
- new: right-click a player at the table - or press and hold on a touch screen - to open a menu with the note, the player profile, ignore and report avatar, the same gesture the desktop and mobile clients offer; "My note" opens the player card straight on the note, with the cursor in the field
- improvement: a plain tap on a seat still opens the full player card, exactly as before - the menu is a shortcut, not a detour

2026-09-07 version 2.1.8-web:
- improvement: your own name is now bold in the players list, so you find your line at a glance

2026-09-07 version 2.1.8-web:
- improvement: in the lobby players list, your rating of a player now sits in the star column instead of after their name - the same column that marks your own row, so the two never collide; the colour dot stays next to the name
- bugfix: stars are gold again in every theme - the PokerTH theme paints its accent white, which turned them white too; they now follow each theme's own gold

2026-09-07 version 2.1.8-web:
- improvement: the colour dot now sits next to the star rating, after the player's name instead of in front of it - both marks about the same player are read together, at the table and in the lobby players list

2026-09-07 version 2.1.8-web:
- improvement: at the table, a player's star rating now sits at the right end of their name line instead of in front of the name, exactly where the desktop and mobile clients put it - the name simply shortens against it, and the colour dot stays in front

2026-09-07 version 2.1.8-web:
- new: your player notes and star ratings now travel in the config.xml export/import, in the same format the desktop and mobile PokerTH clients use - a note written there shows up here, and one written here shows up there
- improvement: importing a config.xml never erases your colour labels; the official format has no field for them, so they simply stay in this client

2026-09-07 version 2.1.8-web:
- improvement: the player card window now keeps one fixed width instead of being stretched sideways - the card is a centred column, so a wide window only added empty margins; the height still adjusts, and the season cups keep their own window

2026-09-07 version 2.1.8-web:
- new: rate a player from zero to five stars, right above your private note on their card - tap the star you want, tap it again to take it back; the rating shows as "★N" in front of their name at the table and in the players list, like the desktop and mobile clients
- improvement: ratings travel with your notes between devices, and the tooltip in front of a name now shows the colour label, the rating and the start of your note together

2026-09-07 version 2.1.8-web:
- bugfix: a faulty or hostile server can no longer flood the browser with avatar data - a picture that is bigger than announced, or that arrives incomplete, is now dropped instead of being kept in memory or cached under the wrong name

2026-09-07 version 2.1.8-web:
- improvement: the client now tells the server which system your browser runs on (Windows, macOS, Linux, Android or iOS), the way the desktop and mobile clients already do - it lets the server operator see which platforms players actually use; nothing else about your browser is sent

2026-09-07 version 2.1.8-web:
- bugfix: connecting to your own LAN or dedicated server works again when the operator forwards player IPs to the game server - that setting now applies only to the server the site is configured for, and no longer to an address you type in yourself

2026-09-06 version 2.1.8-web:
- improvement: the guest and account notices are now hand-written in all 45 languages of the app (using each language's own game-type names) instead of machine-translated - no more odd wording
- bugfix: enabling a notice without touching the pre-filled text no longer freezes it - future improvements to the built-in messages reach everyone automatically

2026-09-06 version 2.1.8-web:
- new: tell everyone what you think of the music - two thumbs under the track title let you say whether you like what is playing, radios included; one vote per device, changeable or removable at any time, and nothing about who voted is recorded
- new: voting is blind by default - you see your own thumb but not the totals, so the numbers on screen cannot sway your answer; the server operator can reveal the counts

2026-09-06 version 2.1.8-web:
- improvement: clearer default notices - the chat sections now explain in plain words why an envelope can be missing (your own line, a guest, someone mid-game), and the account notice describes each community scene (BBC time-based games, WeCup tournaments, the Monthly Cup) with its own ranking

2026-09-06 version 2.1.8-web:
- improvement: the guest and account notices now appear only once - after tapping "I understand" they stay hidden until the operator edits the message; with a pokerth.net account the acknowledgement follows you across devices

2026-09-06 version 2.1.8-web:
- improvement: both default notices now explain when the private-message envelope shows up - only next to players who are online and not seated at a running game

2026-09-06 version 2.1.8-web:
- improvement: the default message for registered accounts now details the ranking scene - ranking games are fixed 10-player tournaments that only start when full, results feed the official pokerth.net ranking, and the community BBC, WeCup and Monthly Cup competitions are mentioned

2026-09-06 version 2.1.8-web:
- improvement: the guest and registered-account notices now ship with a ready-made English text (access, chat, game types, ranking) - it is used until the operator writes their own, and the app translates it into each player's language

2026-09-06 version 2.1.8-web:
- new: the guest notice now has a twin for registered accounts - a popup shown on every connection made with a pokerth.net account, written per language in the admin page and auto-translated for the others

2026-09-06 version 2.1.8-web:
- new: server operators can show a notice to guests - a popup that appears on every connection made as a guest in Internet / PokerTH.net mode, typically explaining what guests can and cannot do compared to a registered account; it is written per language in the admin page and auto-translated for the others

2026-09-06 version 2.1.8-web:
- new: player notes - tap an opponent (seat or players list) to write a private note about them and mark them with one of six colour labels; the colour dot shows in front of their name at the table and in the players list, and hovering it previews your note
- new: the meaning of each colour label can be renamed (for example turn "Danger" into "Bluffs a lot") - the new name applies to every player carrying that colour
- new: notes and labels follow your account between devices - notes taken on the phone and on the desktop are merged, never overwritten

2026-09-04 version 2.1.8-web:
- bugfix: hand-history recording no longer fails when Android closes its database in the background - the client reopens it and carries on; if the device storage is completely full, recording pauses quietly for the session instead of erroring after every hand
- improvement: automatic error reports no longer pick up noise caused by browser extensions (crypto wallets, page translators), so real client issues stand out

2026-09-03 version 2.1.8-web:
- bugfix: with a seat style other than PokerTH, bets are shown next to the player box again, as they were before the bet display option was introduced

2026-09-03 version 2.1.8-web:
- improvement: the bet display choice (inside or next to the player box) now belongs to the PokerTH seat style only - the setting appears under its entry in the Seats tab, and other seat styles keep their own look

2026-09-03 version 2.1.8-web:
- bugfix: with bets shown inside the boxes, the bet of the top-centre player (and of the bottom spectator seat) was half cut off - it now sits fully inside its strip like every other seat, as in the desktop client

2026-09-03 version 2.1.8-web:
- improvement: opponent boxes now have the official fixed width - chip stacks and long names no longer resize the boxes, so the community cards and the action bar keep a constant size as the amounts change, exactly like the desktop client
- bugfix: the highlight that slightly enlarges the box of the player whose turn it is no longer nudges the table layout as the turn moves around

2026-09-03 version 2.1.8-web:
- bugfix: the table no longer shifts when bets appear - with bets shown inside the player boxes, the background image, the community cards and the seat layout could nudge every time a bet strip opened or closed; everything now stays perfectly still, as in the desktop client

2026-09-01 version 2.1.8-web:
- bugfix: connecting to an older LAN or dedicated PokerTH server (before 2.1.8) no longer fails with "Version incompatible" - the client automatically retries by introducing itself the classic way when the server does not know the new web client type
- improvement: the changelog in About now groups each version's entries under New / Improvements / Bug fixes headings, in your language, so it is much easier to scan
- bugfix: when a player voluntarily shows their cards after everyone else folded before the river, the log no longer names a hand - only the cards are shown, as in the desktop client
- improvement: the lobby and game chats now keep the last 400 lines instead of growing forever, so long sessions stay fast, as in the desktop client
- improvement: your own box now lifts slightly when it is your turn, exactly like the official client; its size ratio next to the opponents follows the 2.1.8 measures, and on phones in portrait the bottom opponent row can no longer touch your box
- improvement: dealer and blind pucks now sit exactly where the official 2.1.8 client puts them - left-column seats carry bet and puck on their left in landscape, the top-centre box splits them (bet right, puck left), the spectator's bottom seat does the same, and with bets inside the box the side puck is centred next to the box
- improvement: bigger, better-placed player boxes on phones, as in the official 2.1.8 client - in portrait the seat rows now adapt to the real box size instead of fixed positions, the community cards fill the free middle band, and the space kept next to the boxes matches your bet display style (less is reserved when bets sit inside the box)
- improvement: the welcome message and server announcements are now translated into your language on every browser - they use the same translation service as the chat and the forum when the built-in browser translator is not available; turning off chat translation also keeps them in their original language
- improvement: the local-backup banner now also appears on a brand-new browser, so you can create your first backup right away
- improvement: the banner offering to restore your settings now explains itself - the browser can keep a local backup of your settings in a folder of your choice, with clear Create / Restore / Later buttons; creating a backup never overwrites an existing backup file
- improvement: the web client now follows the PokerTH 2.1.8 release running on pokerth.net
- improvement: the client now introduces itself to the server as a web client for real - the switch shipped earlier but stayed off until the server could recognise it; web players are now counted as their own client type in server statistics, the Live/Spectator tool and the game-server dashboard
- improvement: the stats HUD on the seats is no longer marked as beta - it is an official feature of the web client, still off by default in Advanced options > Assistance
- improvement: bets are now shown inside the player box by default on phones and tablets too, as in the official client since 2.1.8 - the tray under the box needs no room around the boxes, which suits small screens best. Your own choice in Settings > Styles > Seats, "Bet display", is kept as it is
- new: the Ivoire & Chêne table style - a light oak table with an ivory felt and a brass rail, set in a library, with matching cream buttons and pucks. The first light table of the collection; the chat and log panels follow with a warm parchment tint. Settings > Styles > Table
- improvement: the cards of your deck are loaded in the background when you sit down at a table, so the first hands no longer show blank cards for a moment on mobile networks
- improvement: the assistance win percentage is computed in the background and no longer freezes the table for up to a second on phones after the flop, turn and river; it is also a little more precise
- bugfix: the community cards no longer flicker at the turn and the river - cards already on the board stay put and only the new card flips, as in the desktop client; the river card also no longer showed up before the turn card
- improvement: the table previews in Settings > Styles > Table are the freshly rendered ones from the 2.1.8 desktop client - every table now has a landscape and a portrait preview, showing the bets inside the player boxes, and the default table and Green Casino get a real preview instead of a plain felt crop

2026-08-13 version 2.1.7-web:
- new: private messages, like the desktop client - envelope next to a player's name or in the lobby header, conversations listed down the left with message bubbles, translation globe on received messages, one inbox per signed-in account; closed while a hand is running and never sent to guests, exactly as the desktop client does
- new: player profiles now show what the desktop client shows - current season rank, score, average, games and points, the last five finishes, placement breakdown with charts, and the cups of all three rankings; the player card and the game info card are ordinary windows you can move and resize, the lobby stays usable behind them
- new: the players list tells you where someone is playing - rest your mouse on a name, or open the card on a phone; server admins get the kickban hammer directly in the list
- new: 90 emoji reactions instead of 30, on three pages you can flip or swipe - faces, hands and party, and a poker page with the shark, the fish, the chicken, the chip rack, a bomb that really explodes and a water pistol that really fires; reactions now reach the table almost instantly
- new: the Disco table style - a seventies club lounge with mirror ball and neon sign - with the Blacklight 4c card deck and the Disco card back to match (upstream 2.1.8 style packs); and the BBC Anthem table theme with its card back (by BaShFX)
- new: choose where bets are shown - in a small tray under the player box (new default on desktop, like the official client) or next to the box as before (default on phones)
- new: bet keypad - on touch devices it opens inside the action bar so nothing jumps around; on computers it floats just above the action bar. The bet slider moves in the same steps as the desktop client and is much easier to use with a finger
- new: the "your turn" notification carries Fold and Check/Call buttons, so you can play a hand from the notification itself
- new: automatic backup - pick a folder once and the app keeps your backup file up to date there, and offers to restore everything in one click if the browser storage was wiped (desktop Chrome, Edge and Opera); the app also asks the browser for persistent storage
- new: open with / share to PokerTH - a style pack, a backup file, a desktop config.xml or an invitation link sent to the installed app is imported or opened on arrival; invitation links open in the app instead of a browser tab
- new: "invite friends" opens a share window - QR code, copy button, whatsapp/telegram/e-mail/sms shortcuts and the phone share sheet; an invite link shows which table you are invited to right on the login screen, and keeps the server address it came from
- new: custom sounds - replace any game sound with your own audio file in advanced options / sound
- new: report an inappropriate avatar from a player's profile - the same report the desktop client sends, available on phones too
- new: five more interface languages - Indonesian, Thai, Filipino, Bengali and Swahili - 45 in total, help included
- new: the poker rules, hand rankings, how-to-play guide, glossary and FAQ pages are now written in all 45 languages, using the same hand names as the game itself; links between them keep your language, right-to-left languages read right to left, and card sequences always read left to right
- new: three pages you can read without opening the app - hand rankings with the odds of each one, a step-by-step how-to-play guide, and a glossary of fifty-five poker terms; sharing a link to them shows a proper preview card
- new: the "suggest players" button in invitation games now also works for BBC and WEC community admins at their community's tables, and lists one player per line; the Monthly Cup templates fill in the real name of the current tournament
- new: session logs - select several logs and delete them in one go, move through the list with the arrow keys
- new: admin page - automatic updates that wait until nobody is connected, weekly reset for the shared leaderboard, music play counts, per-day language chart and "show all" breakdowns on the traffic page, search-engine settings with AI-crawler refusal and IndexNow
- improvement: the web client now introduces itself to the server as its own client type, so server statistics can tell web players apart
- improvement: the app opens from its local copy first and refreshes in the background, downloads updates a few files at a time, and retries a script that failed to load - pages and tables come up instantly even when the server is briefly busy
- improvement: the game proxy protects itself against connection floods - regular play is unaffected
- improvement: the server's inactivity warning is a proper dialog with a countdown, a sound and an OK button
- improvement: chat and private-message translation falls back to a second service when the first one is temporarily blocked (VPN users especially), and tells you when nothing works at all
- improvement: choosing a photo as your avatar is lighter on memory on phones, and your avatar is prepared once when you pick it instead of being re-processed at every sign-in
- bugfix: losing your connection during a running game no longer costs you your seat - the client sits back down at the next hand, within the five minutes the server holds it
- bugfix: a player who walked away from a table kept their seat for ever - seats are now released after the server's inactivity timer, like the desktop client
- bugfix: the lobby could stay on screen with a frozen table list long after the connection had died - the client now notices and reconnects on its own; retries after a refused connection are properly spaced out so the server no longer blocks the address
- bugfix: signing in on pokerth.net stopped working with a "version not supported" error after a server update
- bugfix: "IP address blocked" when two web players joined the same table - the server now sees each player's real address
- bugfix: the "idle players" view no longer lists spectators, and a spectator's status light shows which table they are watching, like the desktop client
- bugfix: the "suggest players" button had gone quiet - the suggestion was swallowed by the chat cleanup; it shows up again, only for you
- bugfix: on iPhone and iPad, the app installed on the home screen opened on a white screen; the player card and the game info card keep clear of the notch and the status bar
- bugfix: the app sometimes offered an update that was already installed, and a newly imported seat pack only showed up after the offline cache renewed itself
- bugfix: the automatic server update could restart in the middle of a game when the only players connected were on phones with the screen off
- bugfix: the All-In button was hard to read on the Green Casino table in light mode; the Monthly Cup link shows the real Monthly Cup badge; the tabs of the About window no longer break words in the middle on phones

2026-08-05 version 2.1.6-web:
- new: forum news window in the lobby - the latest pokerth.net forum posts are readable in the app, with an unread counter; read state is synced across devices for authenticated accounts
- new: the table admin of a game is shown in the game list, the lobby player lists and the waiting room (QML parity)
- new: the ranking page explains how the PokerTH ranking is calculated (QML parity)
- new: changelog tab on the About page - the one you are reading
- new: default4c card deck - four coloured suits (PokerTH 2.1.6)
- new: table invite links - an "Invite friends" button in the waiting room
- new: opt-in PROXY protocol header, so the game server sees each web player's real address instead of the proxy's
- new: Arabic, Persian, Hebrew and Urdu interface languages - 40 languages in total
- improvement: reconnection after a lost connection fine-tuned - cancel button, seat-reserved notice, readable error reasons, and every retry resumes the old session first (QML 2.1.6 parity)
- improvement: the AFK timeout is reset on real user activity only (QML 2.1.6 parity)
- improvement: ignoring a player also hides their emoji reactions
- improvement: light mode surfaces aligned with PokerTH 2.1.6 - grey page, white content boxes
- improvement: music and sounds survive iOS audio interruptions and network handovers
- improvement: proxy hardening mirroring the upstream server fixes - bounded send queues, ingress validation, avatar decode limits, TLS public-key pinning
- improvement: optional search-engine layer (admin-controlled, off by default) with server-rendered rules and FAQ pages
- bugfix: an intended check could turn into a call when a raise arrived at the same moment (QML 2.1.6 parity)
- bugfix: a pre-armed Check/Call no longer fires as an unintended call after a late raise
- bugfix: connecting in training mode no longer overwrites the remembered LAN server settings
- bugfix: stray background bands in the installed PWA on iOS and iPadOS

2026-07-30 version 2.1.5-web:
- new: vector avatar creator - a step-by-step wizard with a silhouette-coherent catalogue of faces, hair, outfits and accessories
- new: avatar studio window with Gallery, Create and Import tabs
- new: playing-style quadrant on the HUD seat badge; a player's range grid opens straight from their row in the stats table
- improvement: the create-game form is seeded from the player's own preferences and remembers the last table name actually typed
- improvement: join failures are surfaced clearly, with a watchdog for silent WebSocket deaths
- improvement: spectators are view-only - chat and emoji reactions are locked while watching
- bugfix: forum-reported bugs - reconnection, big-blind pre-actions, heads-up board layout

2026-07-22 version 2.1.4-web.0:
- first release under the upstream-aligned version scheme (major.minor.patch follows the PokerTH release, -web.N counts web deployments)
- new: in-app multilingual help - ten chapters covering the whole client, translated into every interface language
- new: in-app debug log view like the QML client's, and hand-log upload through the official pokerth.net analysis pipeline
- new: music playlist and live web radios; stats HUD with ATS and FTS, automatic .pdb hand-log saving
- improvement: QML parity pass on seats (geometry, fold opacity, accents), the action bar (F1-F4 pre-actions, round gates, themed radii) and game-log scrollback
- improvement: full i18n sweep - the last hard-coded strings replaced by translated catalogue entries

Before 2026-07-22 (0.3.x beta series):
- initial development of the web client - game engine bridge, lobby, tables, offline training against bots, PWA shell, style packs and the WebSocket-to-TCP proxy

